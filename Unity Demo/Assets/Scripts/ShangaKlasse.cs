using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class ShangaKlasse : MonoBehaviour
{


    /*
    public Button blå;
    public Button rød;
    public Button kanyle;
    public Button bomull;
    public Button boss;

    public Text text;

    public bool Kanyle;
    public bool Blå;
    public bool Rød;
    public bool Bomull;
    public bool Boss;
  

    // Start is called before the first frame update
    void Start()
    {
        SetTekst("Tekst");

        Kanyle = false;
        Boss = false;
        Blå = false;
        Bomull = false;
        Rød = false;
     

        kanyle.onClick.AddListener(kanyleKlikket);
        blå.onClick.AddListener(blåKlikket);
        rød.onClick.AddListener(rødKlikket);
        boss.onClick.AddListener(bossKlikket);
        bomull.onClick.AddListener(bomullKlikket);
      

    }

    // Update is called once per frame
    void Update()
    {
        
    }

    protected void Button1_Click(object sender, EventArgs e)
    {
        MyTextBox.Text = "Text to display";
    }

    void kanyleKlikket()
    {
        if kanyle;
    }

    //to make it show or hide every time you click,
    public class TextShowHide : MonoBehaviour
    {

        public void ShowHideText(UnityEngine.UI.Text text)
        {
            text.gameObject.active = !text.gameObject.active;
        }
    }

    */

}
